#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../topojson-simplify/bin/toposimplify" "$@"
  ret=$?
else 
  node  "$basedir/../topojson-simplify/bin/toposimplify" "$@"
  ret=$?
fi
exit $ret
