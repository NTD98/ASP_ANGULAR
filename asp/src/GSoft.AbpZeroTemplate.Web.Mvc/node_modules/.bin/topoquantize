#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../topojson/node_modules/topojson-client/bin/topoquantize" "$@"
  ret=$?
else 
  node  "$basedir/../topojson/node_modules/topojson-client/bin/topoquantize" "$@"
  ret=$?
fi
exit $ret
