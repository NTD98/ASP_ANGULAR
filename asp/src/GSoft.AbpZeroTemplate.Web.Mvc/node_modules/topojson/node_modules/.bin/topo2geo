#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../topojson-client/bin/topo2geo" "$@"
  ret=$?
else 
  node  "$basedir/../topojson-client/bin/topo2geo" "$@"
  ret=$?
fi
exit $ret
